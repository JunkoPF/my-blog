import React from 'react';
import styles from './Title.module.scss';

const Title = () => (
    <div className={styles['title']}>
        <span>Polyethylene's Blog</span>
    </div>
)

export default Title;